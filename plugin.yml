name: Shellcheck
description: Run shellcheck over your shell scripts
author: https://github.com/lox
requirements:
  - bash
  - docker
configuration:
  properties:
    files:
      type: [string, array]
      minItems: 1
    options:
      type: [string, array]
      minItems: 1
    version:
      type: [string]
  required:
    - files
